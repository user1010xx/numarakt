[build]
builder = "NIXPACKS"

[deploy]
# -u: stdout anında log (Railway'de "Starting Container" sonrası BOT_ENTRY görünmeli)
startCommand = "python -u bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
